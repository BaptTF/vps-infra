apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vikunja-cert
spec:
  secretName: tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "task.bapttf.com"
