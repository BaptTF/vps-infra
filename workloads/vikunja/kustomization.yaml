apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: vikunja

resources:
  - infisical-secret.yaml
  - pvc.yaml
  - certificate.yaml
  - deployment.yaml
  - service.yaml
  - ingressroute.yaml

configMapGenerator:
  - name: vikunja-config
    files:
      - config.yml
