apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: obsidian-livesync
  namespace: couchdb
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`couchdb.bapttf.com`)
      kind: Rule
      services:
        # Matches the fullnameOverride service name
        - name: couchdb-svc-couchdb
          port: 5984
  tls: {}