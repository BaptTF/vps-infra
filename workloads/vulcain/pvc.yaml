apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vulcain-data
  namespace: vulcain
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
