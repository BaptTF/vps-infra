apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vulcain-cert
  namespace: vulcain
spec:
  secretName: tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "vulcain.bapttf.com"
