apiVersion: v1
kind: Service
metadata:
  name: opencode-dashboard
  namespace: opencode-dashboard
  labels:
    app: opencode-dashboard
spec:
  selector:
    app: opencode-dashboard
  ports:
    - protocol: TCP
      port: 8080
      targetPort: http
      name: http
