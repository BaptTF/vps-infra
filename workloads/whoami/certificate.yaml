apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: whoami-cert
spec:
  secretName: tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "test.bapttf.com"
