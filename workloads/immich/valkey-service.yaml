apiVersion: v1
kind: Service
metadata:
  name: immich-valkey
  namespace: immich
  labels:
    app: immich
    component: valkey
spec:
  selector:
    app: immich
    component: valkey
  ports:
    - name: redis
      port: 6379
      targetPort: redis
