apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ce-analytics-cert
spec:
  secretName: tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "ce-analytics.bapttf.com"
