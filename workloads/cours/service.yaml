apiVersion: v1
kind: Service
metadata:
  name: cours
  namespace: cours
  labels:
    app: cours
spec:
  selector:
    app: cours
  ports:
    - protocol: TCP
      port: 9847
      targetPort: http
      name: http
