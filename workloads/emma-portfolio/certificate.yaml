apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: emma-portfolio-cert
  namespace: emma-portfolio
spec:
  secretName: tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "emma.bapttf.com"