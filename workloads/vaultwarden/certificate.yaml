apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vaultwarden-cert
spec:
  secretName: tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "vault.bapttf.com"
