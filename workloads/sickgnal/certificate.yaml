apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sickgnal-cert
  namespace: sickgnal
spec:
  secretName: sickgnal-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  privateKey:
    algorithm: RSA
    size: 2048
  dnsNames:
    - "sickgnal.bapttf.com"
