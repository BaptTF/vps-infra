apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: camille-recettes-cert
  namespace: camille-recettes
spec:
  secretName: tls-certificate
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "camille-recettes.bapttf.com"
