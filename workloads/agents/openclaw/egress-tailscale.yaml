apiVersion: v1
kind: Service
metadata:
  name: nas-proxy
  namespace: openclaw
  annotations:
    tailscale.com/tailnet-ip: "100.75.160.28"
spec:
  type: ExternalName
  externalName: placeholder  # ignoré quand l'annotation tailscale est présente
  ports:
    - port: 3128
      protocol: TCP
