apiVersion: v1
kind: Service
metadata:
  name: hermes-lya-filebrowser
  namespace: openclaw
spec:
  selector:
    app: hermes-lya-filebrowser
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
