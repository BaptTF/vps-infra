apiVersion: v1
kind: Service
metadata:
  name: steel-browser
  namespace: openclaw
spec:
  selector:
    app: steel-browser
  ports:
    - name: api
      port: 3000
      targetPort: 3000
    - name: debug
      port: 9223
      targetPort: 9223
  type: ClusterIP
