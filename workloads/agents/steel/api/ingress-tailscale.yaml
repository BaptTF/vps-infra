apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: steel-api-ingress
  namespace: openclaw
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - steel-api
  defaultBackend:
    service:
      name: steel-api-service
      port:
        number: 3000