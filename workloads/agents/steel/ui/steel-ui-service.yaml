apiVersion: v1
kind: Service
metadata:
  name: steel-ui-service
  namespace: openclaw
spec:
  selector:
    app: steel-ui
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
