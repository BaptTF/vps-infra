apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - infisical-secret.yaml
  - nullclaw-leo-pvc.yaml
  - nullclaw-lea-bridge-pvc.yaml
  - nullclaw-leo-deployment.yaml
  - nullclaw-service.yaml
  - whatsmeow-bridge-deployment.yaml
  - whatsmeow-bridge-service.yaml

configMapGenerator:
  - name: nullclaw-leo-config
    files:
      - config.json=config.json
