apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: voyage
  namespace: voyage
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`voyage.bapttf.com`)
      kind: Rule
      services:
        - name: voyage
          port: 4180
  tls: {}
