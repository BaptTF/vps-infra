# ARC Controller values
# Lightweight - just needs to watch for AutoscalingRunnerSet CRDs
resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 256Mi

# Flags for the controller manager
flags:
  # Log level (debug, info, warn, error)
  logLevel: info
