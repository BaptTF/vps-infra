apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater
metadata:
  name: assisted-teacher-updater
  namespace: argocd
spec:
  applicationRefs:
    - namePattern: assisted-teacher
      images:
        - alias: assisted-teacher-backend
          imageName: ghcr.io/rjullien/assisted-teacher/backend
          commonUpdateSettings:
            updateStrategy: newest-build
        - alias: assisted-teacher-frontend
          imageName: ghcr.io/rjullien/assisted-teacher/frontend
          commonUpdateSettings:
            updateStrategy: newest-build
  writeBackConfig:
    method: "git:secret:argocd/argocd-image-updater-git-creds"
    gitConfig:
      branch: main
